# releaser

A simple CLI tool to generate and validate Home Cloud release manifests. To run, simply create an output directory and execute the program:

```sh
mkdir out/
go run main.go generate
```

See options with the help flag:

```sh
go run main.go generate --help
```
