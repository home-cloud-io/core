---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: wireguards.home-cloud.io
spec:
  group: home-cloud.io
  names:
    kind: Wireguard
    listKind: WireguardList
    plural: wireguards
    singular: wireguard
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Wireguard is the Schema for the wireguards API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Wireguard
            properties:
              address:
                description: Address specifies the address of the interface in CIDR
                  notation.
                type: string
              listenPort:
                description: ListenPort specifies an interface's listening port.
                type: integer
              name:
                description: Name specifies the name of the interface.
                type: string
              peers:
                description: Peers specifies a list of peer configurations to apply
                  to an interface.
                items:
                  properties:
                    allowedIPs:
                      description: |-
                        AllowedIPs specifies a list of allowed IP addresses in CIDR notation.
                        for this peer.
                      items:
                        type: string
                      type: array
                    endpoint:
                      description: Endpoint specifies the endpoint of this peer entry,
                        if not nil.
                      type: string
                    persistentKeepaliveInterval:
                      description: |-
                        PersistentKeepaliveInterval specifies the persistent keepalive interval
                        for this peer, if not nil.

                        A non-nil value of 0 will clear the persistent keepalive interval.
                      format: int64
                      type: integer
                    presharedKey:
                      description: |-
                        PresharedKey specifies a peer's preshared key configuration, if not nil.

                        Setting to nil will clear the preshared key.
                      properties:
                        dataKey:
                          description: DataKey specifies the data key to find the
                            requested value in.
                          type: string
                        name:
                          description: Name specifies name of the secret.
                          type: string
                        namespace:
                          description: Namespace specifies the namespace of the secret.
                          type: string
                      required:
                      - dataKey
                      - name
                      - namespace
                      type: object
                    privateKeySecret:
                      description: PrivateKeySecret references a Secret which contains
                        the private key for this peer.
                      properties:
                        dataKey:
                          description: DataKey specifies the data key to find the
                            requested value in.
                          type: string
                        name:
                          description: Name specifies name of the secret.
                          type: string
                        namespace:
                          description: Namespace specifies the namespace of the secret.
                          type: string
                      required:
                      - dataKey
                      - name
                      - namespace
                      type: object
                    publicKey:
                      description: |-
                        PublicKey specifies the public key of this peer.  PublicKey is a
                        mandatory field for all PeerConfigs.
                      type: string
                  required:
                  - allowedIPs
                  - publicKey
                  type: object
                type: array
              privateKeySecret:
                description: PrivateKeySecret references a Secret which contains the
                  private key for this interface.
                properties:
                  dataKey:
                    description: DataKey specifies the data key to find the requested
                      value in.
                    type: string
                  name:
                    description: Name specifies name of the secret.
                    type: string
                  namespace:
                    description: Namespace specifies the namespace of the secret.
                    type: string
                required:
                - dataKey
                - name
                - namespace
                type: object
            required:
            - address
            - listenPort
            - name
            - peers
            - privateKeySecret
            type: object
          status:
            description: status defines the observed state of Wireguard
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Wireguard resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
