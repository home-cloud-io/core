---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: installs.home-cloud.io
spec:
  group: home-cloud.io
  names:
    kind: Install
    listKind: InstallList
    plural: installs
    singular: install
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Install is the Schema for the installs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: InstallSpec defines the desired state of Install
            properties:
              version:
                description: Version is the version of the installation.
                type: string
            required:
            - version
            type: object
          status:
            description: InstallStatus defines the observed state of Install
            properties:
              version:
                description: Version is the version of the Install that is currently
                  installed.
                type: string
            required:
            - version
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
