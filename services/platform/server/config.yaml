service:
  name: home-cloud-server
  domain: platform
  entrypoint: http://localhost:2221

  network:
    port: 8000
    internal:
      host: localhost
      port: 8000
    external:
      host: localhost

server:
  k8s:
    config_path: /home/jgkawell/.kube/config
    master_url: https://home-cloud.local:6443
