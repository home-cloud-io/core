service:
  name: home-cloud-server
  domain: platform
  entrypoint: http://localhost:2221

  network:
    port: 8000
    internal:
      host: localhost
      port: 8000
    external:
      host: localhost
