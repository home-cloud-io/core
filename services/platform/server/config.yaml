service:
  name: home-cloud-server
  domain: platform
  entrypoint: http://10.0.0.27:2221

  network:
    port: 8000
    internal:
      host: localhost
      port: 8000
    external:
      host: localhost

server:
  k8s:
    config_path: YOUR_HOME_DIRECTORY/.kube/config
    master_url: https://home-cloud.local:6443
